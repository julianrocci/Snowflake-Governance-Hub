Result cache :
Same query, no data changed inside the table : The result cache is used and instant.
Warehouse don't need to run to re-use the cache.
No cost
Retention 24h

Warehouse cache :
Keep the part of data from tables already requested.
Cache available as long as the warehouse is running