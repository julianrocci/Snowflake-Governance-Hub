name: 'cost_governance_dbt'
version: '1.0.0'

profile: 'default'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

vars:
  credit_price_usd: 4.00

models:
  cost_governance_dbt:
    staging:
      +materialized: view
    intermediate:
      +materialized: "{{ 'view' if target.name == 'dev' else 'ephemeral' }}"
    marts:
      +materialized: table
      +tags: ['daily']